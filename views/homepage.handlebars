{{#each posts as |post|}}
  <div class="post">
    <h3><a href="/post/{{post.id}}">{{post.title}}</a></h3>
    <p class="post-meta">Posted by
      {{post.user.username}}
      on
      {{format_date post.created_at}}</p>
    <p>{{post.body}}</p>
  </div>
{{/each}}